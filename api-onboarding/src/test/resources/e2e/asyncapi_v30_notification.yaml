asyncapi: 3.0.0
info:
  title: Notification API
  description: A sample AsyncAPI 3.0 spec for end-to-end test fixtures.
  version: 1.0.0
  x-api-name: notification
  x-bundle-name: notification
  x-product-name: platform
channels:
  notifications:
    address: platform/notifications
    messages:
      notificationSent:
        $ref: '#/components/messages/NotificationSent'
operations:
  sendNotification:
    action: send
    channel:
      $ref: '#/channels/notifications'
    messages:
      - $ref: '#/channels/notifications/messages/notificationSent'
components:
  messages:
    NotificationSent:
      payload:
        type: object
        properties:
          id:
            type: string
          message:
            type: string
