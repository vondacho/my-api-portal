openapi: 3.0.3
info:
  title: Unnamed API
  description: A spec intentionally missing the required onboarding extension fields.
  version: 1.0.0
servers:
  - url: http://localhost:8080
paths:
  /items:
    get:
      operationId: listItems
      responses:
        '200':
          description: OK
