overlay: 1.0.0
info:
  title: Overlay for injecting scoring data into the API document
  version: 1.0.0
extends: '/openapi.yaml'
actions:
  - target: '$' # Root of document
    update:
      info:
        x-metric-sli: {{sli}}
        x-metric-slo: {{slo}}
